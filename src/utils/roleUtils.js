// Yeh file ab minimal hai — system roles hata diye gaye.
// Sab kuch permission-based hai (navConfig.js + authStore.hasPermission).

export function getDashboardRoute() {
  // Ab ek hi dynamic dashboard hai
  return "/dashboard"
}
